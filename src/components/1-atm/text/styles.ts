import { css } from "@emotion/react";
import { fontSize, colors } from "../../../util/defineCss";

const style = css({
  margin: "0",
  fontSize: fontSize.normal,
  color: colors.font,
});

export { style };
