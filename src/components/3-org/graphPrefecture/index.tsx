const GraphPrefecture = ({
  prefectureCodes,
}: {
  prefectureCodes: string[];
}) => {
  console.log(prefectureCodes); // 値確認用にconsole.logを出力
  return <div>グラフ表示部分</div>;
};

export default GraphPrefecture;
